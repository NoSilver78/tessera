# D5-real-Gate (PR #13, welle-b/d5-recovery-gate) — 2026-06-29
Adversariales Panel (6 Skeptiker) + **Eigenprüfung** (s.u.). Gemerged als `de90c8f`.

## Entscheidung: PASS
D5 ist als **ehrlicher Rescue-Beweis** akzeptiert. No-Admin-Lockout ist real bewiesen; der echte `/config/.storage/auth`-Eingriff bleibt ehrlich **observational/PARTIAL** (kein Wiederholen des Welle-B-Over-Claims). **D5 rückt PARTIAL → PASS.** Gesamturteil bleibt **PARTIAL / kein Enforce-Go** (D7 PARTIAL, A4 BLOCKED, D6-ENFORCE_BYPASS-Lücken offen).

## Verifiziert
- **No-Admin-Lockout = bewiesen.** `no_admin_lockout=True` aus **zwei echten Operate-Probes** (nicht hardcoded; Pre-Seed `None`): `owner_operate_probe.authenticated_and_operated=True` (is_owner=True) **und** `admin_operate_probe.authenticated_and_operated=True` (Admin). Owner + Admin behalten nach dem Rescue Zugang.
- **S2 ehrlich observational.** `d5_pass_lever=False` (hartverdrahtet) geht nur als Blockierbedingung ins Gate — kann nie PASS heben. Der PASS-Pfad liegt auf der Tessera-**Sidecar** (`tessera.config`), **nicht** auf `.storage/auth`. Report sagt das explizit.
- **S1 gemessen.** REPLACE-Demotion-Drift → Boot-Rescue **vor** erzwungener `RuntimeError`-Setup-Exception (strukturell garantiert) → Post-Boot-Reread `exact_match` → `run_id` verkettet prepare→boot→post-boot.
- **Keine Kollaterale.** `owner/admin_before_after_unchanged` gemessen, `owner_system_unmanaged_never_touched=True`.
- **Isolation.** Diff nur `spike/` + `tests/test_d5_verdicts.py` (reiner Unit-Test, kein Produkt-/HA-Import); Produkt enforce-frei.

## ⚠️ Ehrliche Gate-Notiz (Panel-Reliability)
Zwei der sechs Panel-Agenten lieferten **keine validen Ergebnisse** (Modell-Instabilität an dem Tag): der **No-Admin-Lockout-Verifier** gab ein degeneriertes „test"-Ergebnis, der **others-untouched-Verifier** überschritt den StructuredOutput-Retry-Cap. Die Synthese überbrückte das über die Cross-Coverage der S1/S2-Agenten; **zusätzlich habe ich (Claude) die beiden schwachen Claims selbst gegen Harness-Code + Evidence-JSON geprüft** (s. „Verifiziert" oben) — beide halten. Das PASS stützt sich also auf 4 saubere Agenten **+ Eigenprüfung der 2 kritischen Lücken**, nicht auf das nominelle `problemCount:0` allein.

## Auflagen (nicht-blockierend, Hardening — in den Welle-E-Rebase mitnehmbar)
1. Feld **`auth_store_corrupted` umbenennen** (z.B. `managed_group_replace_drift_injected`) — irreführend, meint managed-REPLACE-Drift, nicht `.storage/auth`-Byte-Korruption.
2. **Seeds entliteralisieren/kommentieren** (`owner_system_unmanaged_never_touched=True` @754, `rescue_restore_namespace_guarded=True` @761) — damit Seed nicht mit gemessenem Gate-Wert verwechselt wird.
3. **Guard-Tests in CI verankern** (`tests/test_d5_verdicts.py`, inkl. „S2-never-creates-PASS") — Fail-Safe-Charakter von S2 regressions-schützen.
